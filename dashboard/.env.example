# WebSocket URL for the MiniStore StatsServer
# For local dev, it falls back to ws://localhost:8090, but you can explicitly set it here.
# For production (e.g. Render), set this to your hosted backend URL (e.g. wss://ministore-engine.onrender.com)
VITE_WS_URL=ws://localhost:8090
