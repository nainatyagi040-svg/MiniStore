# Dashboard Environment Configuration Example

# The URL the React dashboard uses to connect to the StatsServer WebSocket.
# Useful if hosting on a different domain/port.
# VITE_WS_URL=ws://localhost:8090
